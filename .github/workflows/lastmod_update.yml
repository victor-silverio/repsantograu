name: Update Metadata (Sitemap & Humans)

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  update-metadata:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: |
          pip install -r scripts/automation/requirements.txt
          npm ci

      - name: Run Metadata update
        run: python scripts/automation/lastmod_update.py

      - name: Format code
        run: npm run format

      - name: Commit and Push if changed
        run: |
          git config --global user.name 'Metadata BOT'
          git config --global user.email 'seobot@repsantograu.online'
          git add public/sitemap.xml public/humans.txt
          git diff --quiet && git diff --staged --quiet || (git commit -m "update lastmod" && git push)
